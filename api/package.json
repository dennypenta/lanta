{
  "name": "@lanta/api",
  "type": "module",
  "scripts": {
    "dev": "bun index.ts",
    "start": "bun index.ts",
    "migrate:generate": "drizzle-kit generate",
    "migrate:migrate": "drizzle-kit migrate",
    "auth:generate": "better-auth generate --config ../auth.config.ts --output auth/schema.ts",
    "auth:migrate": "better-auth migrate --config ../auth.config.ts"
  },
  "dependencies": {
    "@lanta/shared": "workspace:*",
    "better-auth": "^1.4.10",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.4.10",
    "better-sqlite3": "^12.6.0",
    "drizzle-kit": "^0.31.8"
  }
}

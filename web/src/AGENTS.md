## project structure
- components holds all the jsx components
- components/ui holds basic ui blocks, they have no state, pure ui elements
- components/widgets - composition of ui blocks, has a behaviour, state
- components/pages - composition of widgets, a router targets to this components


